name: Foundation Repository Gates

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "23 5 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  foundation:
    uses: baobab-platform/shared/.github/workflows/foundation-repository-gates.yml@f7466cc89a17c0b6e02b5a1e5fce3ce76073a718
    with:
      dockerfile: ""
      build_context: "."
      dependency_review_enabled: false
