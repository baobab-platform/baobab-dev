name: Foundation Repository Gates

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "23 5 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  foundation:
    uses: nabhold/shared/.github/workflows/foundation-repository-gates.yml@919ee0ade2047017a0915d9f22566381a6655dff
    with:
      dockerfile: ""
      build_context: "."
      dependency_review_enabled: false
